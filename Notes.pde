/*****************************************************
 * AIR MASH-UP APPLICATION NOTES
 * 
 * Planned Updates
 * - create a flag that identifies when a hand is added or removed from the sensor
 * - ignore volume changes greater than a certain threshold when moving volume up or down gradually
 * - create a flag that identifies the current intent of the user (up or down) when a hand changes direction from going up to down, or down to up
 * - add a second sensor to the sensor array
 *
 * Physical Ideas
 * - consider adding pedals that enable you to tell the sensor whether your gesture is absolute or gradient
 * - add a laser to identify where the sensors are pointed
 * - add an led that turns on when it senses a hand is sensed (and turns off when it leaves the sensor) 
 * 
 *
 * Notes about how gestures are being tracked:
 * - look for gestures that 
 ********************************************************/
